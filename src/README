В этой секции расположены файлы отладки алгоритма микроконтроллера.

На вход подается файл в формате *.bin, после чего происходит расчет параметров ориентации и запись в текстовый файл.

Все типы, используемые в *.c и *.h файлах аналогичны типам в файлах прошивки микроконтроллера, 
предоставляемые компанией STMicroelectronics. Таким образом, после отладки, файлы добавляются в проект прошивки без изменений.
